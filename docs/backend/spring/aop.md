---
title: Spring AOP
description: 理解 Spring AOP 的切点、通知、代理对象和常见应用场景
outline: deep
---

# Spring AOP

AOP 是面向切面编程，适合处理多个模块都会遇到的横切逻辑，例如日志、权限、事务、限流和性能统计。

## 核心概念

| 概念 | 说明 |
| --- | --- |
| 切点 | 匹配哪些方法需要增强 |
| 通知 | 增强逻辑本身 |
| 切面 | 切点和通知的组合 |
| 连接点 | 可以被增强的方法调用点 |
| 代理 | 包装目标对象并织入增强逻辑的对象 |

## 代理机制

Spring AOP 通常通过代理实现。调用方拿到的是代理对象，代理对象先执行切面逻辑，再调用目标方法。

```txt
调用方 -> 代理对象 -> 切面逻辑 -> 目标方法
```

这也是为什么同一个类内部方法自调用时，某些 AOP 增强可能不生效。

## 常见场景

- 事务控制。
- 接口访问日志。
- 权限校验。
- 方法耗时统计。
- 幂等和限流。

## 注意点

- 切面逻辑不要过重，避免影响所有被匹配方法。
- 切点范围要准确，不要误伤无关方法。
- 要理解代理调用边界，尤其是事务和自调用问题。

## 总结

AOP 的价值是把重复横切逻辑从业务代码中抽出来。理解代理机制后，事务不生效、日志重复、切点匹配错误等问题会更容易定位。
